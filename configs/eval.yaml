seed: 42
paths:
  checkpoint_path: checkpoints/pretrained/cn_spoil_ecml_pkdd_2026.pt
data:
  eval_path: data/CN-Spoil/dev.jsonl
  max_samples: null
model:
  text_dim: 128
  visual_dim: 128
  hidden_dim: 128
